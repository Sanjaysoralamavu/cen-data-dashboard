{
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 3000",
    "build": "node scripts/generate-responses-json.cjs && vite build && node scripts/generate-static-pages.cjs",
    "preview": "vite preview --host 127.0.0.1 --port 3000"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "react-ga4": "^3.0.1",
    "vite": "latest"
  }
}
